# Environment variables (never commit real keys)
.env
# Runtime server state (persisted poll settings)
poll-settings.json
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
auth.*
Google*
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
api/order-statuses.db

# IdP metadata XML (contains certificates — keep out of source control)
*IDPMetadata.xml
